apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-watcher-main
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-main
        IS_API_MODULE_ENABLED: true
      image:
        name: blockchain-watcher
        tag: "2.78.0-17-gf0761a33" # {"$imagepolicy": "apps:blockchain-watcher-main-image-policy:tag"}
