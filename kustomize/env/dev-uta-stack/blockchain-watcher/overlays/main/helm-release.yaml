apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-watcher-main
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-main
        IS_API_MODULE_ENABLED: true
      image:
        name: blockchain-watcher
        tag: "2.77.0-22-g86edf21b" # {"$imagepolicy": "apps:blockchain-watcher-main-image-policy:tag"}
