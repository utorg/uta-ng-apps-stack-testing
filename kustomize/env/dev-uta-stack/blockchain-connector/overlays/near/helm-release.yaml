apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-near
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-near
        INSTANCE_CHAINS: NEAR
      image:
        name: "blockchain-connector"
        tag: "2.78.0-6-ge49411d5" # {"$imagepolicy": "apps:blockchain-connector-near-image-policy:tag"}
