apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-optimism
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-optimism
        INSTANCE_CHAINS: OPTIMISM
      image:
        name: "blockchain-connector"
        tag: "2.84.0-3-g0762720c" # {"$imagepolicy": "apps:blockchain-connector-optimism-image-policy:tag"}
