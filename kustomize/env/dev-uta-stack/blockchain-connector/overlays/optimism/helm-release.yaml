apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-optimism
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-optimism
        INSTANCE_CHAINS: OPTIMISM
      image:
        name: "blockchain-connector"
        tag: "2.78.0-16-ga0d45eb9" # {"$imagepolicy": "apps:blockchain-connector-optimism-image-policy:tag"}
