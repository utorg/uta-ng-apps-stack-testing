apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-arbitrum
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-arbitrum
        INSTANCE_CHAINS: ARBITRUM
      image:
        repository: "europe-west4-docker.pkg.dev/dev-uta-ng-stack/dev-uta-ng-stack-cr/blockchain-connector" # {"$imagepolicy": "apps:blockchain-connector-arbitrum-image-policy:name"}
        tag: "2.52.0-204-ged8cc015" # {"$imagepolicy": "apps:blockchain-connector-arbitrum-image-policy:tag"}
