apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-arbitrum
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-arbitrum
        INSTANCE_CHAINS: ARBITRUM
      image:
        name: "blockchain-connector"
        tag: "2.86.0-14-g6e918b95" # {"$imagepolicy": "apps:blockchain-connector-arbitrum-image-policy:tag"}
