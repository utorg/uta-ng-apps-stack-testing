apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-linea
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-linea
        INSTANCE_CHAINS: LINEA
      image:
        name: "blockchain-connector"
        tag: "2.85.0-28-g67cea304" # {"$imagepolicy": "apps:blockchain-connector-linea-image-policy:tag"}
