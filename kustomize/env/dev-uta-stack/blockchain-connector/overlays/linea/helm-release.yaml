apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-linea
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-linea
        INSTANCE_CHAINS: LINEA
      image:
        name: "blockchain-connector"
        tag: "2.86.0-14-g6e918b95" # {"$imagepolicy": "apps:blockchain-connector-linea-image-policy:tag"}
