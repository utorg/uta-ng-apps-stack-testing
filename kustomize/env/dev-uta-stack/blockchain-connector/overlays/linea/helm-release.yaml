apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-linea
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-linea
        INSTANCE_CHAINS: LINEA
      image:
        name: "blockchain-connector"
        tag: "2.78.0-7-gb33dc26a" # {"$imagepolicy": "apps:blockchain-connector-linea-image-policy:tag"}
