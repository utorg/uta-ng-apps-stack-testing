apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-ton
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-ton
        INSTANCE_CHAINS: TON
      image:
        name: "blockchain-connector"
        tag: "2.83.0-0-g5e163f74" # {"$imagepolicy": "apps:blockchain-connector-ton-image-policy:tag"}
