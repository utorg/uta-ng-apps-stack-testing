apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-ton
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-ton
        INSTANCE_CHAINS: TON
      image:
        name: "blockchain-connector"
        tag: "2.85.0-28-g67cea304" # {"$imagepolicy": "apps:blockchain-connector-ton-image-policy:tag"}
