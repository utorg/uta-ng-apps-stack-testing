apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-ethereum
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-ethereum
        INSTANCE_CHAINS: ETHEREUM
      image:
        name: "blockchain-connector"
        tag: "2.78.0-15-g9a73b2ed" # {"$imagepolicy": "apps:blockchain-connector-ethereum-image-policy:tag"}
