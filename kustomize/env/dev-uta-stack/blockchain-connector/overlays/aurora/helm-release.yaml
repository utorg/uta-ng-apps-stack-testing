apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-aurora
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-aurora
        INSTANCE_CHAINS: AURORA
      image:
        name: "blockchain-connector"
        tag: "2.77.0-5-gd8216abe" # {"$imagepolicy": "apps:blockchain-connector-aurora-image-policy:tag"}
