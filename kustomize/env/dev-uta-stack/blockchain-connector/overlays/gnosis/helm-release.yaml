apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-gnosis
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-gnosis
        INSTANCE_CHAINS: GNOSIS
      image:
        name: "blockchain-connector"
        tag: "2.85.0-28-g67cea304" # {"$imagepolicy": "apps:blockchain-connector-gnosis-image-policy:tag"}
