apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-gnosis
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-gnosis
        INSTANCE_CHAINS: GNOSIS
      image:
        name: "blockchain-connector"
        tag: "2.89.0-7-g36b531c5" # {"$imagepolicy": "apps:blockchain-connector-gnosis-image-policy:tag"}
