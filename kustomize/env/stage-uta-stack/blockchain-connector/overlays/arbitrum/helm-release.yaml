apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-arbitrum
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-arbitrum
        INSTANCE_CHAINS: ARBITRUM
      image:
        name: "blockchain-connector"
        tag: "2.84.0-2-gcc4c98ff" # {"$imagepolicy": "apps:blockchain-connector-arbitrum-image-policy:tag"}
