apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-zksync
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-zksync
        INSTANCE_CHAINS: ZKSYNC
      image:
        name: "blockchain-connector"
        tag: "2.88.0-11-gec981547" # {"$imagepolicy": "apps:blockchain-connector-zksync-image-policy:tag"}
