apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-zksync
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-zksync
        INSTANCE_CHAINS: ZKSYNC
      image:
        name: "blockchain-connector"
        tag: "2.84.0-2-gcc4c98ff" # {"$imagepolicy": "apps:blockchain-connector-zksync-image-policy:tag"}
