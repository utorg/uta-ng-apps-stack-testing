apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-ethereum
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-ethereum
        INSTANCE_CHAINS: ETHEREUM
      image:
        name: "blockchain-connector"
        tag: "2.89.0-14-g982d2d39" # {"$imagepolicy": "apps:blockchain-connector-ethereum-image-policy:tag"}
