apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-optimism
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-optimism
        INSTANCE_CHAINS: OPTIMISM
      image:
        name: "blockchain-connector"
        tag: "2.89.0-14-g982d2d39" # {"$imagepolicy": "apps:blockchain-connector-optimism-image-policy:tag"}
