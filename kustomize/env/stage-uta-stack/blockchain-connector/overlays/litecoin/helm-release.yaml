apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-litecoin
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-litecoin
        INSTANCE_CHAINS: LITECOIN
      image:
        name: "blockchain-connector"
        tag: "2.84.0-2-gcc4c98ff" # {"$imagepolicy": "apps:blockchain-connector-litecoin-image-policy:tag"}
