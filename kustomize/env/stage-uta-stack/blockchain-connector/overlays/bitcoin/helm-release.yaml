apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-bitcoin
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-bitcoin
        INSTANCE_CHAINS: BITCOIN
      image:
        name: "blockchain-connector"
        tag: "2.85.0-3-gc941e0de" # {"$imagepolicy": "apps:blockchain-connector-bitcoin-image-policy:tag"}
