apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-fantom
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-fantom
        INSTANCE_CHAINS: FANTOM
      image:
        name: "blockchain-connector"
        tag: "2.88.0-11-gec981547" # {"$imagepolicy": "apps:blockchain-connector-fantom-image-policy:tag"}
