apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blockchain-connector-linea
spec:
  values:
    deployment:
      env:
        APPLICATION_NAME: blockchain-connector-linea
        INSTANCE_CHAINS: LINEA
      image:
        name: "blockchain-connector"
        tag: "2.84.0-2-gcc4c98ff" # {"$imagepolicy": "apps:blockchain-connector-linea-image-policy:tag"}
